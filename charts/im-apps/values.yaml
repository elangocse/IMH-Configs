image:
  repository: dxctraining.azurecr.io/apps/myapp
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 80
  targetPort: 8080
  type: ClusterIP

ingress:
  enabled: true
  className: nginx
  host: imh-app-dev.example.com
  path: /
  tls: false
  tlsSecretName: ""

resources: {}
containerPort: 8080